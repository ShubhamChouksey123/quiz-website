# Environment variables for local development

# Database Configuration
DB_ADMIN_PASSWORD=localdev123
DB_NAME=quiz
DB_USER=postgres

# For local development (app running outside Docker, connecting to Docker PostgreSQL)
QUIZ_DB_HOST=localhost
QUIZ_DB_PORT=5432
QUIZ_DB_NAME=quiz
QUIZ_DB_USERNAME=postgres
QUIZ_DB_PASSWORD=localdev123

# Email Configuration (dummy values for local testing)
GMAIL_USERNAME=test@example.com
GMAIL_PASSWORD=test-password